location ^~ /nzbhydra/api {
    # API Name (for logging)
    set $api_name "NZBHydra2";

    # Authentication
    include api_auth.conf;

    # Proxy options
    resolver 127.0.0.11 valid=30s;
    set $upstream_nzbhydra hydra2;
    proxy_pass http://$upstream_nzbhydra:5076;
}