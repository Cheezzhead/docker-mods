location ^~ /sabnzbd/api {
    # API Name (for logging)
    set $api_name "SabNZBd";

    # Authentication
    include api_auth.conf;

    # Proxy options
    resolver 127.0.0.11 valid=30s;
    set $upstream_sabnzbd sabnzbd;
    proxy_pass http://$upstream_sabnzbd:8080;
}